# Environment variables
.env

# SQLite Database (we don't want to push user data or let Streamlit overwrite it if we decide to mount a volume later)
crypto_app.db

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Temporary data files generated by models
final_merged_data.csv
model_predictions.csv
feature_importance.png

# Virtual environment (if you have one)
venv/
env/
.venv/

# Streamlit specific
.streamlit/
